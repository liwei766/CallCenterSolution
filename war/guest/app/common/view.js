"use strict";

/*
 * 共通の画面制御処理（CCS.view）
 *
 * ※prop.js、util.js の後
 */

( function( CCS ) {

	var _prop = CCS.prop;
	var _util = CCS.util;

	var ret = {

		name : "CCS.view",

	};	// end of ret

	CCS.view = ret;

} )( CCS ) ;

